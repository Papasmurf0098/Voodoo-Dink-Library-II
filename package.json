{
  "name": "voodoo-drink-library",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check js/app.js && node --check sw.js && npm test"
  },
  "engines": { "node": ">=22.22.2" },
  "devDependencies": { "jsdom": "30.1.0" }
}
